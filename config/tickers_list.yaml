# config/tickers_list.yaml

# ── app settings ─────────────────────────────────────────────
start_date: "2012-01-01"
date_format: "%Y-%m-%d"
long_process_refresh_seconds: 30

# ── tickers ──────────────────────────────────────────────────
tickers:
  # US large cap / ETFs, strong history, good seasonal candidates
  - AAPL
  - MSFT
  - NVDA
  - HD
  - FDX
  - PAYX
  - GIS        # General Mills, consumer staples, textbook seasonality
  - NVO        # Novo Nordisk

  # sector ETFs — known seasonal patterns
  - GDX        # gold miners
  - CGW        # water infrastructure
  - XLE        # energy, strong winter/summer cycle
  - GLD        # gold, macro seasonal
  - XLV        # healthcare, defensive seasonal

  # growth / thematic
  - PLTR
  - CRWD
  - IONQ
  - QS
  - SMCI

  # speculative (kept for contrast, expect weak/NaN metrics)
  - BB
  - BMBL

  # international
  - 7974.T     # Nintendo — Tokyo
  - MONC.VI    # Moncler — Vienna
  - BAS.DE     # BASF — Frankfurt, energy-linked
  - ALV.DE     # Allianz — Frankfurt, Q4 dividend cycle
